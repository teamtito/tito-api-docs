# Releases

## Attributes

Attribute | Type | Description
--------- | ---- | -----------
<%- data.releases.attributes.each do |name, attributes| -%>
<%= name %> | `<%= attributes["type"] %>` | <%= attributes["description"] %>
<%- end -%>

## Get all Releases

<%= partial "includes/requests/get", locals: { url: "https://api.tito.io/v2/an-account/awesome-conf/releases" } %>

> The above command returns JSON structured like this:

<%= partial 'includes/releases/index' %>

This endpoint retrieves all `Releases` for the given `Event`.

### HTTP Request

`GET https://api.tito.io/v2/:account/:event/releases`

<aside class="notice">
Note. The attributes for each Release have been removed from the example JSON response for readability.
</aside>

## Get a specific Release

<%= partial "includes/requests/get", locals: { url: "https://api.tito.io/v2/an-account/awesome-conf/releases/early-bird-ticket" } %>

> The above command returns JSON structured like this:

<%= partial 'includes/releases/show' %>

This endpoint retrieves a specific `Release`.

### HTTP Request

`GET https://api.tito.io/v2/:account/:event/releases/:release`

## Creating a new Release

<%= partial "includes/requests/post", locals: {
      url: "https://api.tito.io/v2/an-account/awesome-conf/releases",
      parameters: { slug: 'Awesome-conf', title: 'Awesome Conf' }
    } %>

This endpoint creates a `Release` on the given `Event`.

### HTTP Request

`POST https://api.tito.io/v2/:account/:event/releases`

### Parameters

Parameter | Type | Description
--------- | ------- | -----------
<%- data.releases.attributes.each do |name, attributes| -%>
<%= name %> | `<%= attributes["type"] %>` | <%= attributes["description"] %>
<%- end -%>

## Updating a Release

<%= partial "includes/requests/patch", locals: {
      url: "https://api.tito.io/v2/an-account/awesome-conf/workshop-ticket",
      parameters: {
        data: {
          type: "releases",
          id: "workshop-ticket",
          attributes: {
            quantity: 25
          }
        }
      }
    } %>

This endpoint updates a `Release` on the given `Event`.

### HTTP Request

`PATCH https://api.tito.io/v2/:account/:event/:release`

### Parameters

Parameter | Type | Description
--------- | ---- | -----------
<%- data.releases.attributes.each do |name, attributes| -%>
<%= name %> | `<%= attributes["type"] %>` | <%= attributes["description"] %>
<%- end -%>

## Deleting a Release

<%= partial "includes/requests/delete", locals: {
      url: "https://api.tito.io/v2/an-account/awesome-conf/workshop-ticket"
    } %>

This endpoint deletes a `Release` on the given `Event`.

### HTTP Request

`DELETE https://api.tito.io/v2/:account/:event/:release`
